#!/bin/bash

# Run containers
docker-compose up -d

docker exec -it training_vue_node_1 /bin/bash -c "cd training_vue && rm -rf node_modules package-lock.json && npm install"
docker exec -d training_vue_node_1 /bin/bash -c "cd training_vue && npm run serve"
