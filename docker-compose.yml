version: '3'
services:
  node:
    build:
      context: .
      dockerfile: ./docker/node/Dockerfile
    tty: true
    environment:
      - NODE_ENV=development
    volumes:
      - ./:/app
    command: sh -c "cd training_vue && npm run serve"
    ports:
      - "8080:8080"
